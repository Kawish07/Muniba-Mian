node_modules
dist
# Edit your .gitignore to add these critical lines
Add-Content -Path .gitignore @"

# Server environment files
server/.env
server/.env.*
server/*.env

# Uploads and media files (should be in cloud storage, not Git)
server/uploads/
server/public/uploads/
server/temp/

# Or be more specific if you want to keep some images
# !src/Public/images/keep-this-one.jpg
"@
